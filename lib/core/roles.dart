/// Convenzione ruoli allineata al backend SlowFit.
///
/// client  = 1  (utente finale: quiz/register + login)
/// trainer = 2  (personal trainer: solo login)
class Roles {
  static const int client = 1;
  static const int trainer = 2;

  const Roles._();
}
